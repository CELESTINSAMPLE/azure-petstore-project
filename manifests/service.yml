apiVersion: v1
kind: Service
metadata:
    name: celestinsampleazurepetstoreproject
spec:
    type: LoadBalancer
    ports:
    - port: 80 
    selector:
        app: celestinsampleazurepetstoreproject